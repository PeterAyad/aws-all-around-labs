# Steps

1. **Pick** a self-hosted service from <https://selfh.st/apps/>
2. **Create** a public EC2 instance in the default VPC, and create SSH key pair
3. **Connect** to the instance using SSH, and install the self-hosted service
4. **Test** by visiting the instance's public IP in a web browser
